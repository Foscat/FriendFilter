import React from 'react';
import Rotate from "../Rotate";

const Loader = (props) => {
    return(
        <Rotate>
            <img alt="logo" src="./images/favicon+bcrypt.jpg" />
        </Rotate>
    )
}

export default Loader;